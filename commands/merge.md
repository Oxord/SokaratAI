1. Объедини все комиты, которые были сделаны в feature-ветку с задачкой в один комит
2. Пушь последний комит в remote ветку.
3. Открывай PR и сливай его в main-ветку. Если проект FitnessTogetherBackend, то сливай в релизную ветку `release/v1.0` (именно в неё смотрит прод-деплой; ветка `qa` больше не используется).
4. После успешного merge переключайся в ветку-базу (main для обычных проектов, release/v1.0 для FitnessTogetherBackend).